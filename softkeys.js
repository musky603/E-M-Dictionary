window.addEventListener('keydown', function(e) {
   switch(e.key) {
    
     case '8': //back
	   window.history.back();
       
       break;
     case '9': //exit

	   window.close();
         //          open(location, '_self').close();
       
       break;
	   
    
   }})
  
